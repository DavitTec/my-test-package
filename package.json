{
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "serve out",
    "export": "next export",
    "deploy": "next build && next export",
    "ghpages": "rm -rf node_modules/.cache && next build && next export && touch out/.nojekyll && git add out/ && git commit -m \"@DavitTec Deploy to gh-pages\" && git subtree push --prefix out origin gh-pages"
  },
  "dependencies": {
    "next": "latest",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "serve": "^13.0.2"
  },
  "devDependencies": {
    "babel-plugin-transform-define": "^2.0.1"
  }
}
